# NetworkingTwo topologies via `pulumi config set engine analytics|db`:| | analytics (default) | db ||---|---|---|| Network | Public DNS endpoint, IAM-auth | Private VPC + bastion SSH tunnel || Bastion | No | Yes (`t3.micro`, needs keyName + myIp config) || SSH into Neptune | Never — Neptune is fully managed by AWS, not an EC2 | Never — same; bastion is YOUR EC2 to SSH to, then forwards TCP:8182 || Bedrock KB vector store | Only Neptune engine Bedrock accepts | Not in Bedrock API enum |Notebook `%%graph_notebook_config` block per engine — see `infra/pulumi/README.md`.External docs: <https://github.com/aws/graph-notebook#connecting-graph-notebook-locally-to-amazon-neptune> and <https://docs.aws.amazon.com/bedrock/latest/APIReference/API_agent_StorageConfiguration.html>.<!-- The previous 228-line version with ASCII topology diagrams was cut. Run git show HEAD~3:natural_language_querying/NETWORKING.md if you need it. -->